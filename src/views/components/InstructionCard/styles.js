import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles(() => ({
  notePaperItem: {
    display: 'flex',
    alignItems: 'center',
    cursor: 'pointer',
  },
}));

export { styles };
